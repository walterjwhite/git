#!/bin/sh

. _LIBRARY_PATH_/install/logging.sh

# install hooks
if [ ! -e ~/.git-hooks ]
then
    exitWithError "~/.git-hooks configuration does NOT exist, unable to continue" 1
fi

. ~/.git-hooks
cp hooks/* $_SYSTEM_HOOK_PATH