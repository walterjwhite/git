#!/bin/sh

# install hooks
if [ ! -e ~/.config/walterjwhite/git ]
then
    exitWithError "~/.git-hooks configuration does NOT exist, unable to continue" 1
fi

. ~/.config/walterjwhite/git
tar cpf - -C hooks . | tar xpf - -C $_SYSTEM_HOOK_PATH
